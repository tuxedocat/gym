import XCTest
@testable import MaxDepthBST

final class MaxDepthBSTTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(MaxDepthBST().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
